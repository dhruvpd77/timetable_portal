# Deployment files

- **wsgi_pythonanywhere.py** – Template for the PythonAnywhere Web app WSGI config. Replace `YOUR_USERNAME` and `YOUR_SECRET_KEY`, then paste into the WSGI configuration file in the Web tab.

See the project root **DEPLOYMENT.md** for the full step-by-step PythonAnywhere deployment guide.
